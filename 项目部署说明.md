# 家庭记账本 - 前后端分离项目部署说明

## 🎯 项目概述

本项目是一个完整的家庭记账本应用，采用前后端分离架构：

- **前端**: React 19 + TypeScript + Ant Design 5
- **后端**: NestJS + TypeORM + SQLite (可扩展为MySQL)
- **认证**: JWT Token认证机制
- **数据库**: SQLite (开发环境) / MySQL (生产环境)

## 📋 功能特性

### ✅ 已完成功能

#### 用户认证系统
- 用户注册和登录
- JWT Token自动管理
- 路由保护（未登录自动跳转）
- Token过期自动处理

#### 交易管理
- 添加收入/支出记录
- 交易分类管理
- 实时统计和图表
- 多维度筛选（类型、分类、日期）
- 数据导入导出（CSV格式）

#### 技术特性
- 响应式设计，支持移动端
- TypeScript类型安全
- 前后端分离架构
- 模块化代码结构

## 🚀 快速启动

### 方式一：使用启动脚本（推荐）

#### Windows系统
```cmd
# 双击运行 start.bat
start.bat
```

#### Linux/Mac系统
```bash
# 给脚本执行权限
chmod +x start.sh

# 运行脚本
./start.sh
```

### 方式二：手动启动

#### 1. 安装依赖
```bash
# 安装前端依赖
npm install

# 安装后端依赖
cd backend
npm install
cd ..
```

#### 2. 配置环境变量
```bash
# 复制环境配置文件
cp backend/.env.example backend/.env

# 编辑配置文件（可选）
# 默认配置已足够开发使用
```

#### 3. 启动服务
```bash
# 终端1：启动后端服务
cd backend
npm run start:dev

# 终端2：启动前端服务
npm run dev
```

## 🌐 访问地址

- **前端应用**: http://localhost:5173
- **后端API**: http://localhost:3001

## 🔧 项目结构

```
family-finance-tracker/
├── backend/                 # NestJS后端
│   ├── src/
│   │   ├── auth/           # 认证模块
│   │   ├── users/          # 用户模块
│   │   ├── transactions/   # 交易模块
│   │   └── app.module.ts  # 主模块
│   ├── package.json
│   └── .env               # 环境配置
├── src/                    # React前端
│   ├── components/         # UI组件
│   ├── contexts/          # React Context
│   ├── hooks/             # 自定义Hooks
│   ├── services/          # API服务
│   └── App.tsx            # 主应用
├── package.json
└── README.md
```

## 🔐 API接口文档

### 认证接口

#### 用户注册
```http
POST /auth/register
Content-Type: application/json

{
  "username": "testuser",
  "email": "test@example.com", 
  "password": "password123"
}
```

#### 用户登录
```http
POST /auth/login
Content-Type: application/json

{
  "email": "test@example.com",
  "password": "password123"
}
```

### 交易接口（需要认证）

#### 获取交易列表
```http
GET /transactions
Authorization: Bearer <token>
```

#### 创建交易
```http
POST /transactions
Authorization: Bearer <token>
Content-Type: application/json

{
  "title": "工资收入",
  "amount": 5000,
  "type": "income",
  "category": "工资",
  "date": "2024-01-15"
}
```

#### 获取统计信息
```http
GET /transactions/summary
Authorization: Bearer <token>
```

## 🗄️ 数据库配置

### 开发环境（SQLite）
- 数据库文件：`backend/database.sqlite`
- 自动创建表结构
- 数据持久化存储

### 生产环境（MySQL）
修改 `backend/src/app.module.ts`：

```typescript
TypeOrmModule.forRoot({
  type: 'mysql',
  host: 'localhost',
  port: 3306,
  username: 'your_username',
  password: 'your_password',
  database: 'family_finance',
  entities: [__dirname + '/**/*.entity{.ts,.js}'],
  synchronize: true, // 生产环境建议设为false
}),
```

## 🛠️ 开发指南

### 前端开发
```bash
# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

### 后端开发
```bash
# 进入后端目录
cd backend

# 启动开发服务器（支持热重载）
npm run start:dev

# 构建生产版本
npm run build

# 启动生产版本
npm run start:prod
```

## 📊 数据模型

### 用户表 (User)
```sql
id: number (主键)
username: string (唯一)
email: string (唯一)  
password: string (加密)
createdAt: Date
updatedAt: Date
```

### 交易表 (Transaction)
```sql
id: number (主键)
title: string
amount: number
type: 'income' | 'expense'
category: string
date: Date
description?: string
userId: number (外键)
createdAt: Date
updatedAt: Date
```

## 🔍 故障排除

### 常见问题

1. **端口冲突**
   - 前端端口：5173 → 修改 `vite.config.ts`
   - 后端端口：3001 → 修改 `backend/.env`

2. **数据库连接失败**
   - 检查SQLite文件权限
   - 确认数据库文件路径正确

3. **CORS错误**
   - 确保前端地址在后端CORS白名单中
   - 检查 `backend/src/main.ts` 中的CORS配置

4. **依赖安装失败**
   - 清除node_modules重新安装
   - 检查Node.js版本（需要18+）

### 调试技巧

1. **前端调试**
   - 使用React Developer Tools
   - 查看浏览器控制台错误信息
   - 检查网络请求状态

2. **后端调试**
   - 查看控制台日志输出
   - 使用Postman测试API接口
   - 检查数据库连接状态

## 📈 扩展建议

### 功能扩展
1. **数据可视化** - 集成ECharts图表库
2. **多用户支持** - 家庭多成员账户管理
3. **预算管理** - 设置月度预算和提醒
4. **数据备份** - 云存储同步功能
5. **移动端APP** - React Native版本

### 技术优化
1. **性能优化** - 虚拟滚动、懒加载
2. **安全性** - 输入验证、SQL注入防护
3. **监控告警** - 错误监控、性能监控
4. **自动化部署** - CI/CD流水线

## 📄 许可证

MIT License - 可自由使用、修改和分发

---

**维护提示**: 
- 定期更新依赖包版本
- 关注安全公告和漏洞修复
- 保持代码库的健壮性和安全性
- 定期备份重要数据